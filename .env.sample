
# Region Configuration
REGION_NAME=us-east-1
IS_PRIMARY_REGION=true
SYNC_INTERVAL_MS=5000
HEALTH_CHECK_INTERVAL_MS=30000

# Database Configuration
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your_password
DATABASE_NAME=your_database

# Application Configuration
PORT=3000
NODE_ENV=development
APP_URL=http://localhost:3000

# JWT Configuration
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# Email Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email
SMTP_PASS=your_password
SMTP_FROM=noreply@example.com

# Starknet Configuration
STARKNET_NETWORK=mainnet # or testnet
STARKNET_PROVIDER_URL=https://your-starknet-provider.com
TOKEN_CONTRACT_ADDRESS=your_token_contract_address
TOKEN_CONTRACT_ABI=your_token_contract_abi
NFT_CONTRACT_ADDRESS=your_nft_contract_address
NFT_CONTRACT_ABI=your_nft_contract_abi

# Rate Limiting Configuration
THROTTLE_TTL=60000
THROTTLE_LIMIT=3
THROTTLE_LONG_TTL=3600000
THROTTLE_LONG_LIMIT=5


PERSPECTIVE_API_KEY=your_key
GOOGLE_VISION_API_KEY=your_key
REDIS_URL=redis://localhost:6379

# Firebase Configuration
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=your_client_email
FIREBASE_PRIVATE_KEY=your_private_key

# APNS Configuration
APNS_KEY=your_apns_key
APNS_KEY_ID=your_key_id
APNS_TEAM_ID=your_t

# Elasticsearch Configuration
ELASTICSEARCH_NODE=http://localhost:9200
ELASTICSEARCH_USERNAME=elastic
ELASTICSEARCH_PASSWORD=your_password

# Redis Configuration (for caching and Bull queue)
REDIS_CACHE_URL=redis://localhost:6379
REDIS_QUEUE_URL=redis://localhost:6379

# Search Configuration
SEARCH_INDEX_PREFIX=app
SEARCH_RESULT_LIMIT=100
SEARCH_SUGGESTION_CACHE_TTL=300

# WebSocket Configuration
WS_PORT=3001
WS_PATH=/socket.io
